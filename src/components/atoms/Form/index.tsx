import { styled } from '@mui/material/styles';

const Form = styled('form')(() => ({
    width: '100%',
    maxWidth: 800,
    margin: '0 auto',
    color: '#1e212c',
    borderColor: '#6600c6',
}));

export default Form;
